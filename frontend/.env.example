# Backend API URL (leave empty to use same origin via proxy in dev)
VITE_API_URL=http://localhost:8000

# Admin API key (must match API_SECRET_KEY in backend .env)
VITE_API_KEY=your_admin_api_key_here
